package com.example.skycast;

import android.appwidget.AppWidgetProvider;

public class SkyCastWidgetProvider extends AppWidgetProvider {
    // This is just a receiver shell. Android requires it to register the widget.
    // Our SkyCastAlertService will push the actual UI updates!
}